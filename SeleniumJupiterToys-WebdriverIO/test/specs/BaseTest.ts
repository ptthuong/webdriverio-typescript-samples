export abstract class BaseTest {

    public before() {
        // browser.url('http://jupiter.cloud.planittesting.com');
    }

    public after() {
        // (browser.getWindowHandles()).forEach(async handle => {
        //     browser.switchToWindow(handle);
        //     browser.closeWindow();
        // });
    }
}